const config={
    database: "AppStore",
    server: "DESKTOP-EDHLCDI",
    driver: "msnodesqlv8",
    options: {
        trustedConnection: true,
        encrypt: false,   
    }
}

module.exports = config;